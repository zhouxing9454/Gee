<!-- templates/arr.tmpl -->
<!--迭代Map时可以设置变量，变量以$开头：-->

<html>
<body>
    <p>hello, {{.title}}</p>
    {{range $index, $ele := .stuArr }}
    <p>{{ $index }}: {{ $ele.Name }} is {{ $ele.Age }} years old</p>
    {{ end }}
</body>
</html>